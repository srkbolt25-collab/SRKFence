Updated 19 PDF product pages action buttons.

Changes:
- Changed hero CTA text from "Request Quote" to "Request a Quote".
- Added a second "Add RFQ" button beside it.
- The Add RFQ button uses the same RFQ context/localStorage/toast flow as the Products archive page.
- The button resolves the current PDF product page slug back to the real product ID from the products API when available, so the RFQ cart receives the same product ID used on the Products page.
- Existing PDF content, GCC content, FAQ content, thumbnails, product archive order, and metadata were not changed.
