export const metadata = {
  title: 'SRK Fence API',
  description: 'Backend API for SRK Fence',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

