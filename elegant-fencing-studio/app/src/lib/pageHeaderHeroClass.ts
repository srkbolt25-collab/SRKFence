/**
 * Shared fixed hero band — same dimensions as the Applications listing hero.
 * Use with PageHeader `fixedHero` and optional `innerClassName={PAGE_HEADER_FIXED_HERO_INNER_CLASS}`.
 */
export const PAGE_HEADER_HERO_FIXED_SIZE_CLASS =
  "h-[280px] min-h-[280px] max-h-[280px] sm:h-[320px] sm:min-h-[320px] sm:max-h-[320px] md:h-[360px] md:min-h-[360px] md:max-h-[360px] lg:h-[400px] lg:min-h-[400px] lg:max-h-[400px]";

export const PAGE_HEADER_FIXED_HERO_INNER_CLASS =
  "py-6 sm:py-8 md:py-10 lg:py-12";
